language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-wat' ]; then cd .. && eval "mv $currentfolder generator-wat" && cd generator-wat; fi

